#include <stdio.h>

int main(){
    int a,b,c;

    a = 5;
    b =323;
    c = 444;

    printf("Just trying to make sure I dont lose touch\n");
    printf("%d/(%d * %d) print\n", a,(b * a));
    printf("%d\n",444);
    printf("%d*%d/%d", a,b,c,a*b/c);//gotta be a faster way for this concept
}
