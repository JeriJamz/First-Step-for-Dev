#include <stdio.h>

int main(){
    int county_tax, state_tax;
    county_tax = .04;
    state_tax = .07;
    printf("Taxes went up have you seen the its %d percent now", county_tax + state_tax);
}